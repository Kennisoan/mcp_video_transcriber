# MCP Video Transcriber Environment Configuration
# Copy this file to .env and update the values

# REQUIRED: Your video transcription API endpoint
API_BASE_URL=https://your-video-api.com/api

# REQUIRED: Public URL where your server will be accessible
# This should match your domain configured in nginx proxy manager
SERVER_URL=https://your-domain.com

# SECURITY: JWT signing secret (generate a strong random string)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-here

# OPTIONAL: JWT token expiration in minutes (default: 60)
JWT_EXPIRES_MINUTES=60

# OPTIONAL: Authorization code expiration in minutes (default: 10)
AUTH_CODE_EXPIRES_MINUTES=10

# OPTIONAL: Database path (default: /app/data/oauth.db)
# DATABASE_PATH=/app/data/oauth.db

# OPTIONAL: Server host and port (defaults work for Docker)
# HOST=0.0.0.0
# PORT=8000 